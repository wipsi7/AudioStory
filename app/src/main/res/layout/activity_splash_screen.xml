<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/appBackground"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="fi.metropolia.audiostory.activities.SplashScreenActivity"
    android:id="@+id/splash_screen_activity">

    <ImageView
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:src="@drawable/logo"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop"
        android:id="@+id/splash_screen_activity_iv_logo"
        android:layout_above="@+id/splash_screen_activity_app_name"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="25dp"
        android:contentDescription="@string/desc_logo" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/splash_screen_app_name"
        android:textSize="40sp"
        android:textColor="@color/active_pink"
        android:layout_above="@+id/splash_screen_activity_fl_splash"
        android:layout_centerHorizontal="true"
        android:gravity="center_horizontal"
        android:id="@+id/splash_screen_activity_app_name"
        android:layout_marginBottom="30dp" />

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:id="@+id/splash_screen_activity_fl_splash">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:src="@drawable/splash"
            android:adjustViewBounds="true"
            android:scaleType="centerCrop"
            android:id="@+id/splash_screen_activity_iv_splash"
            android:contentDescription="@string/desc_spalsh" />
    </FrameLayout>

</RelativeLayout>
